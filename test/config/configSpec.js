const config = require('../../src/config/config');

describe('Test for the config module', function() {
    it('should result in getting all files glob', function() {
        expect(true).toBe(true);
    });
});